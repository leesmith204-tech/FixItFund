{
  "name": "fixitfund-proxy",
  "version": "1.0.0",
  "description": "FixItFund — Anthropic API proxy server with rate limiting and CORS",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "nodemon server.js",
    "test":    "node -e \"require('./server.js')\" && echo 'Syntax OK'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors":               "^2.8.5",
    "dotenv":             "^16.4.5",
    "express":            "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "node-fetch":         "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
